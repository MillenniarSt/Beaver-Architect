{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "process",
  "description": "Capability for the process windows",
  "windows": ["process"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default",
    "dialog:default",
    "fs:default"
  ]
}